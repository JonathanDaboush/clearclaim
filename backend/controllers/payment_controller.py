# PaymentController handler functions
def handle_create_subscription(user_id: str, tier: str, start_date, end_date) -> dict:
    """
    Handler for creating subscription.
    Args: user_id, tier, start_date, end_date. Returns: dict result.
    """
    pass

def handle_record_payment(user_id: str, amount: float, method: str, metrics: dict) -> dict:
    """
    Handler for recording payment.
    Args: user_id, amount, method, metrics. Returns: dict result.
    """
    pass
