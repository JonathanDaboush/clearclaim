# Permission utilities

def check_permission(user_id, action, resource):
    """Check if user has permission for action on resource."""
    pass

def can_sign_contract(user_id, contract_id):
    """Check if user can sign contract."""
    pass

def can_add_evidence(user_id, contract_id):
    """Check if user can add evidence to contract."""
    pass

def can_delete_evidence(user_id, contract_id):
    """Check if user can delete evidence from contract."""
    pass

def can_revise_contract(user_id, contract_id):
    """Check if user can revise contract."""
    pass

def can_approve_revision(user_id, contract_id):
    """Check if user can approve contract revision."""
    pass
